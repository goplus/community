import (
	"embed"
	"io/fs"

	"github.com/goplus/yap"
)

//go:embed yap
var yapFS embed.FS

fsYap := fs.sub(yapFS, "yap")!
y := yap.new(fsYap)

y.GET "/p/:id", ctx => {
	ctx.YAP 200, "article", yap.H{
		"id": ctx.param("id"),
	}
}

y.run ":8080"
